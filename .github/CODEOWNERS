# Review is required on the files where a subtle change can disable a check
# without failing a single test. A green suite is not evidence that the gates
# still work - that is precisely the failure mode this project argues against.

* @HaiderHanif

# Golden traces are executable expectations. Editing one silently changes what
# "correct" means for every future run.
/examples/**/traces/ @HaiderHanif

# CI configuration decides whether any gate is enforced at all.
/.github/ @HaiderHanif

# Integrity, redaction and injection scanning are the security surface.
/src/agentgate/integrity.py @HaiderHanif
/src/agentgate/redaction.py @HaiderHanif
/src/agentgate/injection.py @HaiderHanif

# The checks themselves, and the determinism harness they rely on.
/src/agentgate/assertions.py @HaiderHanif
/src/agentgate/constraints.py @HaiderHanif
/src/agentgate/determinism.py @HaiderHanif

/SECURITY.md @HaiderHanif
